#pragma once

#include "common.hpp"
#include "parser_defs.hpp"

/*
 * This is the first version of the Virtual Machine(VM).
 * At the current stage, it supports only text-based interpretation.
 * 
 * Support for interpretation of binary code will be added later.
 */
struct VirtualMachine
{
};
